int a, b, e[2], f;
static char c, d, *g = &c, **h = &g;
char i(char j, char k) { return k ? j % k : 0; }
int main() {
	  int *l = &e[1];
	    for (; d < 1; d++) {
		        int *m = &a, *o = &e[1];
			    **h = l == m;
			        char ***n[2] = {&h, &h};
				    for (; b < 4; ++b)
					          if (f != 1)
							          *o = i(1, *g);
				      }
	      return 0;
}
